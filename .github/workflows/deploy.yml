name: Deploy

on:
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: xavierfenaux-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install SSH key
        run: |
          install -m 700 -d ~/.ssh
          printf '%s\n' "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -T 20 -H 76.13.48.6 >> ~/.ssh/known_hosts || true

      - name: Deploy website
        run: |
          rsync -az --delete \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='.env' \
            --exclude='.well-known/acme-challenge' \
            --timeout=60 \
            -e "ssh -i ~/.ssh/deploy_key -o BatchMode=yes -o ConnectTimeout=20 -o ServerAliveInterval=15 -o ServerAliveCountMax=2 -o StrictHostKeyChecking=accept-new" \
            ./ root@76.13.48.6:/var/www/xavierfenaux/
          ssh -i ~/.ssh/deploy_key -o BatchMode=yes -o ConnectTimeout=20 -o StrictHostKeyChecking=accept-new \
            root@76.13.48.6 "printf '%s\n' '$GITHUB_SHA' > /var/www/xavierfenaux/.deploy-revision"
